{{- $environment := printf "files/.env.%s" .Values.environment -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}
data:
  {{- (.Files.Glob $environment).AsConfig | nindent 2 }}   