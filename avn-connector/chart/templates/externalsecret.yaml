apiVersion: "kubernetes-client.io/v1"
kind: ExternalSecret
metadata:
  name: eth-provider-credentials
spec:
  backendType: secretsManager
  data:
    - key: eth_provider
      name: eth_provider_url
      property: eth_provider_url

{{- if eq .Values.nodeEnv "production" }}
---
apiVersion: "kubernetes-client.io/v1"
kind: ExternalSecret
metadata:
  name: rabbit-credentials
spec:
  backendType: secretsManager
  data:
    - key: rabbitmq
      name: server
      property: server
{{- end }}
---
apiVersion: "kubernetes-client.io/v1"
kind: ExternalSecret
metadata:
  name: vault-role
spec:
  backendType: secretsManager
  data:
    - key: vault
      name: role-id
      property: app_role_id
    - key: vault
      name: secret-id
      property: app_secret_id
---
apiVersion: "kubernetes-client.io/v1"
kind: ExternalSecret
metadata:
  name: rds-credentials
spec:
  backendType: secretsManager
  data:
    - key: gateway_rds_logins
      name: gateway_app_user
      property: gateway_app_user
    - key: gateway_rds_logins
      name: gateway_app_database
      property: gateway_app_database
    - key: gateway_rds_logins
      name: gateway_app_user_password
      property: gateway_app_user_password
    - key: gateway_rds_logins
      name: gateway_rds_host
      property: gateway_rds_host
    - key: gateway_rds_logins
      name: gateway_app_schema_sync
      property: gateway_app_schema_sync
