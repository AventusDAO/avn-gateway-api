ingress-nginx:
  tcp:
    "5672": dev/rabbit-rabbitmq:5672
  controller:
    kind: DaemonSet
    
    service:
      external:
        enabled: false
      internal:
        enabled: true
        annotations:
          # Create internal ELB
          service.beta.kubernetes.io/aws-load-balancer-internal: "true"
          service.beta.kubernetes.io/aws-load-balancer-name: nginx-ingress-controller
