secretManagerRegion: $ENV:SM_AWS_REGION
mqSecretArn: $ENV:MQ_SECRET_ARN
mqBrokerAmqpEndpoint: $ENV:MQ_BROKER_AMQP_ENDPOINT
components:
  avnTxQueue: $ENV:AVN_TX_QUEUE
  avnTxRetryCount: $ENV:AVN_TX_RETRY_COUNT
  avnTxRetryDelay: $ENV:AVN_TX_RESEND_DELAY
  deadLetterQueue: $ENV:DEAD_LETTER_QUEUE
  deadLetterExchange: $ENV:DEAD_LETTER_EXCHANGE
  deadLetterKey: $ENV:DEAD_LETTER_KEY