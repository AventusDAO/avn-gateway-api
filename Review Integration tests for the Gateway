Review Integration tests for the Gateway:

- review correctness of tests, and that parameters are reasonable
- ensure that we can modify the configuration parameters for each environment, and where they're defined
- create a testnet configuration and run tests against it: smoke and integration
- refactor test configuration profile to make it easier and more modular
- ensure that test environment can be applied to _all_ test types

================================================================================

Test Folders:

Test Types:
    smokeTests: ./tests/gateway-smoke-tests.js --> reads config from argv[5]
    apiTests (integration): ./avn-api/tests --> a full folder.
        helper reads config file from argv[6]
        other files just require helper
    avn-connector tests (integration): ./avn-connector/tests --> a full folder.
        mq tests not written
        question: shall we remove these tests?
    lambda tests (integration): ./lambdas/tests --> a full folder.
        empty. There isn't even a stub implementation


Suites:
    allSandbox: api + avnConnector + lambda
    cba: api + avnConnector + lambda