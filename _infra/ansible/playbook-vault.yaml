---
- name: gather facts
  hosts: bastion

- name: Deploy & setup vault server files
  hosts: vault_server

  roles:
  - role: aventus.ansible-avn-vault
    when: deploy_vault|bool
  - role: vault-init
    when: init_vault|bool
